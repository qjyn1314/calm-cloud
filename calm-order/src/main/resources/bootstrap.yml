server:
    port: 84
spring:
    application:
        name: calm-order
    profiles:
        include: common
        active: dev
    cloud:
        nacos:
            discovery:
                server-addr: 47.105.33.140:8848
            config:
                server-addr: 47.105.33.140:8848
                file-extension: properties
# 启动项目之前必须先配置其配置文件
# 添加配置时其DataID的公式：${spring.application.name}-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension} ，即：项目名称-当前环境.文件后缀